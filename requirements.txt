# server.py — Qwen edit endpoint, ComfyUI-backed adapter.
#
# The heavy stack (torch, ComfyUI) lives in the container; the adapter itself
# only speaks HTTP. See docker/ for the full runtime.
fastapi
uvicorn
requests
pillow
pydantic
